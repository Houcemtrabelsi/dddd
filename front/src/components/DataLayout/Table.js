// This component displays data in a tabular format, with rows and columns.
//  It can include features like sorting, filtering, and pagination to enhance data exploration.
//  Each row represents a data item, and each column represents a specific attribute or property of the data.

import styled from "styled-components";

const Table = styled.table`
  /* Styles for the table component */
`;

export default Table;
